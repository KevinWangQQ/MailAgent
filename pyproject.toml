[project]
name = "mailagent"
version = "3.0.0"
description = "macOS Mail.app to Notion real-time sync system"
requires-python = ">=3.9"
readme = "README.md"

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-asyncio>=0.21",
]
